# SciHub · 科研工作台 —— 协作规则

本文件是仓库的协作与安全约束。

## 删除与数据安全

1. 不得删除、覆盖、移动用途不明或来源不明的文件。
2. 任何删除操作都必须先获得用户明确授权；删除前列出待删除文件及原因，未经确认只能建议，不能执行。
3. 不要使用 `rm -rf`、大范围通配符删除或未逐项核对的递归删除。
4. 事实性用户数据存放在 Supabase（`research_records` 等）；改动数据库结构属于不可逆操作，必须先说明影响并获得授权。

## 密钥与边界

1. 前端只允许出现 Supabase 的 `publishable` / `anon` key。**`service_role` key、数据库连接串、任何私钥都不得进入本仓库或前端代码。**
2. 页面的一切数据可见性依赖 Supabase RLS 策略；新增表必须同时给出 `enable row level security` 与「只能读写自己行」的策略。
3. 不得把用户数据写入公开静态文件。

## 提交与验证

1. 提交前检查暂存差异，确认不含密钥、凭据、缓存、临时文件与无关改动。
2. 前端改动至少在本地静态服务器上打开验证：登录/注册、新建/编辑/删除、刷新后数据仍在。
3. `supabase_schema.sql` 与 `app.js` 的表名、字段名必须保持一致，改动其一需同步另一处。
4. 修改前端资源后，同步更新 `version.json` 与 `sw.js` 中的缓存版本号，避免用户拿到旧缓存。
5. 验证失败、目标分支不明确或远程不可用时不要提交/推送，直接报告阻塞原因。
