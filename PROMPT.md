# SciHub 产品实现提示词

你是一名擅长科研工具、信息架构与本地优先软件的资深产品工程师。请实现一个名为 **SciHub** 的中文科研对话知识库原型。它不是普通聊天记录备份工具，而是帮助研究者把多个 AI 模型对话中的有用信息沉淀为可追溯、可验证、可复用的研究资产。

## 产品目标

用户可以导入来自 ChatGPT、Claude、Gemini 等模型的对话记录，将有价值的消息摘录为知识卡片，按研究项目管理，并通过搜索重新找到信息。每一个结论都要能回溯到原始对话及其证据，明确区分“模型声称”和“已验证事实”。

## 本版范围

实现一个本地优先、无需后端即可运行的单页 Web 原型。所有数据保存于浏览器 `localStorage`，初次打开提供可信的演示数据。界面语言为简体中文。

必须实现：

1. 仪表盘：显示项目数、已导入对话数、知识卡片数、待验证项目；展示最近活动、待验证卡片和快捷操作。
2. 项目：按项目浏览与切换研究资料。
3. 对话导入：支持选择 JSON 或文本文件；尽可能读取常见字段（`title`、`messages`、ChatGPT 导出的 `mapping`），保留模型名称和时间；无法识别时也要创建可查看的原始文本对话。
4. 对话查看：以消息流展示用户与 AI 消息，可从任意一条消息创建知识卡片。
5. 知识卡片：支持“研究结论、研究方法、提示词、代码片段、文献线索、待办”六类；卡片展示项目、来源模型、标签、验证状态、原始对话来源和创建日期。
6. 验证状态：未核验、待验证、已验证、存疑。状态必须明显可见，避免把 AI 内容误认为事实。
7. 搜索与筛选：可按关键词搜索，并按卡片类型、验证状态和项目筛选。
8. 创建卡片：提供表单，可输入标题、内容、类型、项目、标签、验证状态；创建后立刻出现在列表和本地数据中。

## 体验与视觉要求

- 气质应安静、专业、适合长期研究工作；避免“通用 AI 聊天”视觉。
- 使用暖白背景、深墨文字、鼠尾草绿作为主色，以低饱和琥珀色提示待验证；版式精致、有充足留白。
- 响应式布局；桌面端使用左侧工作区导航与顶部搜索，窄屏能自然堆叠。
- 提供明确的空状态、导入状态、成功提示与可理解的提示文案。
- 不虚构真实的论文验证结果。演示内容必须清楚标为示例或待验证。

## 工程约束

- 仅用原生 HTML、CSS、JavaScript，不依赖构建工具或第三方 CDN。
- 模块清晰，数据模型易替换为 SQLite/Tauri 等正式实现。
- 使用语义化 HTML 和可访问的按钮、标签、弹窗。
- 交付时说明如何运行，以及下一步接入真实本地数据库和各模型导入器的路线。
